<h2>New task</h2>

<%= render "form.html", changeset: @changeset,
                        all_lists: all_lists,
                        list_id:   @list_id,
                        action: task_path(@conn, :create) %>

<%= link "Back", to: task_path(@conn, :index) %>