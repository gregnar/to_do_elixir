<h2>New task</h2>

<%= render "form.html", changeset: @changeset,
                        all_lists: @all_lists,
                        action: task_path(@conn, :create) %>

<%= link "Back", to: task_path(@conn, :index) %>